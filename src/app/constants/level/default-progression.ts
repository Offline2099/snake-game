import { LevelProgression } from '../../types/level/level-progression.interface';

export const DEFAULT_PROGRESSION: LevelProgression = {
  isLocked: true,
  isComplete: false,
  bestProgress: 0,
  bestTime: 0
}