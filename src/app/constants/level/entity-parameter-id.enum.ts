export enum EntityParameterId {
  initialAmount = 1,
  spawnOnInteraction,
  protectedMargin
  // initialAmount = 'initial-amount',
  // spawnOnInteraction = 'spawn-on-interaction',
  // protectedMargin = 'protected-margin'
}